import React from 'react';
import {SafeAreaView, View, TextInput} from 'react-native';

const Login = () => {
  return (
    <SafeAreaView>
      <View>
        <TextInput />
      </View>
    </SafeAreaView>
  );
};

export default Login;
